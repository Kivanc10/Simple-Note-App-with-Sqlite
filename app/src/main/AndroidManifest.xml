<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kvnc.authwords">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:noHistory="true"
        android:theme="@style/Theme.AuthenticationWithWords">
        <activity android:name=".SetNote" android:parentActivityName=".Inside">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Inside" />
        </activity>

        <activity android:name=".CRUDNote" android:parentActivityName=".Inside">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Inside" />
        </activity>

        <activity android:name=".Inside" android:parentActivityName=".LoginPage">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".LoginPage" />
        </activity>

        <activity android:name=".LoginPage" android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>